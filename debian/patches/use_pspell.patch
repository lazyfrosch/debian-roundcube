Instead of using Google by default, we use pspell to avoid to send each mail to Google...

Index: roundcube/config/defaults.inc.php
===================================================================
--- roundcube.orig/config/defaults.inc.php	2015-03-23 22:23:11.461141439 +0100
+++ roundcube/config/defaults.inc.php	2015-03-23 22:24:17.825102635 +0100
@@ -668,7 +668,7 @@
 // Since Google shut down their public spell checking service, the default settings
 // connect to http://spell.roundcube.net which is a hosted service provided by Roundcube.
 // You can connect to any other googie-compliant service by setting 'spellcheck_uri' accordingly.
-$config['spellcheck_engine'] = 'googie';
+$config['spellcheck_engine'] = 'pspell';
 
 // For locally installed Nox Spell Server or After the Deadline services,
 // please specify the URI to call it.
